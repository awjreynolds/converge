# Extension Host fixture

This intentionally minimal local workspace sets `converge.codexPath` to a missing executable. The
Extension Host suite can therefore exercise Converge's public activation and session commands
without starting Codex or contacting an external service.
